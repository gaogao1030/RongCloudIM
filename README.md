== README


Get Start:

* Ruby version
  2.2.0

* System dependencies
  Ubuntu||Mac OS

* How to run the test suite
```bash
$ bundle install
$ cd client && npm install
$ npm start dev
$ cd .. && rails s
Open http://localhost:3000 in your browser
